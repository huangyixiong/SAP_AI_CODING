{
  "name": "sap-ai-coding",
  "private": true,
  "scripts": {
    "build:mcp": "npm run build --prefix mcp-abap-abap-adt-api",
    "dev": "npm run build:mcp && concurrently --names \"后端,前端\" --prefix-colors \"cyan,yellow\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix mcp-abap-abap-adt-api"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
